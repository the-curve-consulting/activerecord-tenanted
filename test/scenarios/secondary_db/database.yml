#
#  this test config has a tenanted secondary database named "tenanted"
#
test:
  shared:
    adapter: "sqlite3"
    database: "%{storage}/%{env}/%{prefix}/shared.sqlite3"
    migrations_paths: "%{db_path}/shared_migrations"
  tenanted:
    tenanted: true
    adapter: "sqlite3"
    database: "%{storage}/%{env}/%{prefix}/tenanted/%%{tenant}/main.sqlite3"
    migrations_paths: "%{db_path}/tenanted_migrations"
