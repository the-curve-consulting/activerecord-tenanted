#
#  this test config has a tenanted secondary database named "tenanted"
#
test:
  shared:
    adapter: "sqlite3"
    database: "%{storage}/test/%{prefix}/shared.sqlite3"
    migrations_paths: "%{db_path}/shared_migrations"
  tenanted:
    tenanted: true
    adapter: "sqlite3"
    database: "%{storage}/test/%{prefix}/tenanted/%%{tenant}/main.sqlite3"
    migrations_paths: "%{db_path}/tenanted_migrations"
